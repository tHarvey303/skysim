services:
  skysim:
    build: .
    ports:
      - "8000:8000"
    environment:
      - JAX_PLATFORM_NAME=cpu
    # Mount data directory if you have precomputed tables locally
    volumes:
      - ./skysim/data:/app/skysim/data:ro
    restart: unless-stopped
